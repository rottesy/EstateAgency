# Анализ нарушений послойной архитектуры

## Общая оценка

Архитектура проекта в целом соответствует принципам послойной архитектуры, но есть несколько нарушений, которые стоит исправить для улучшения поддерживаемости и тестируемости кода.

---

## ✅ Что соответствует архитектуре

1. **Доступ к данным через EstateAgency**
   - UI виджеты получают доступ к данным через `EstateAgency::getInstance()`
   - Нет прямого создания менеджеров в UI

2. **Централизация файловых операций**
   - Все операции с файлами выполняются через `FileManager`
   - Менеджеры не знают о файлах напрямую

3. **Разделение ответственности**
   - Сущности содержат только данные
   - Менеджеры содержат бизнес-логику
   - UI отвечает за представление

---

## ❌ Нарушения послойной архитектуры

### 1. MainWindow напрямую зависит от всех менеджеров

**Проблема:**
```cpp
// include/ui/MainWindow.h
#include "../services/AuctionManager.h"
#include "../services/ClientManager.h"
#include "../services/FileManager.h"
#include "../services/PropertyManager.h"
#include "../services/TransactionManager.h"
```

**Последствия:**
- Нарушение принципа инверсии зависимостей
- UI слой знает о внутренней структуре сервисного слоя
- Сложнее тестировать и мокать

**Решение:**
- Убрать все прямые includes менеджеров из `MainWindow.h`
- Оставить только `#include "../core/EstateAgency.h"`
- Все операции выполнять через методы `EstateAgency`

---

### 2. MainWindow ловит исключения менеджеров напрямую

**Проблема:**
```cpp
// src/ui/MainWindow.cpp
catch (const PropertyManagerException &e) { ... }
catch (const ClientManagerException &e) { ... }
catch (const TransactionManagerException &e) { ... }
catch (const AuctionManagerException &e) { ... }
catch (const FileManagerException &e) { ... }
```

**Последствия:**
- UI слой знает о конкретных типах исключений сервисного слоя
- При добавлении нового менеджера нужно изменять UI код
- Нарушение инкапсуляции

**Решение:**
- Создать общий базовый класс исключений `ServiceException`
- Или обрабатывать исключения на уровне `EstateAgency`
- UI должен ловить только общие исключения

---

### 3. UI использует dynamic_cast для определения типов сущностей

**Проблема:**
```cpp
// src/ui/PropertiesWidget.cpp
if (const auto *apt = dynamic_cast<const Apartment *>(prop)) { ... }
else if (const auto *house = dynamic_cast<const House *>(prop)) { ... }
else if (const auto *comm = dynamic_cast<const CommercialProperty *>(prop)) { ... }
```

**Последствия:**
- UI слой знает о конкретных типах сущностей
- Нарушение принципа подстановки Лисков (LSP)
- При добавлении нового типа недвижимости нужно изменять UI код

**Решение:**
- Использовать Visitor Pattern для обработки разных типов
- Или добавить виртуальные методы в базовый класс `Property`
- Или использовать полиморфизм через методы типа `getTypeSpecificInfo()`

---

### 4. EstateAgency возвращает прямые ссылки на менеджеры

**Проблема:**
```cpp
// include/core/EstateAgency.h
PropertyManager &getPropertyManager() { return propertyManager; }
ClientManager &getClientManager() { return clientManager; }
// и т.д.
```

**Последствия:**
- UI получает прямой доступ ко всем методам менеджеров
- Нет контроля над тем, какие операции может выполнять UI
- Сложнее добавить дополнительную логику (логирование, валидацию)

**Решение:**
- Добавить фасадные методы в `EstateAgency` для всех операций
- Например: `addProperty()`, `findProperty()`, `removeProperty()` и т.д.
- UI должен работать только через эти методы

---

### 5. UI виджеты напрямую включают заголовки сущностей

**Проблема:**
```cpp
// src/ui/PropertiesWidget.cpp
#include "../../include/entities/Apartment.h"
#include "../../include/entities/Client.h"
#include "../../include/entities/CommercialProperty.h"
#include "../../include/entities/House.h"
#include "../../include/entities/Transaction.h"
```

**Последствия:**
- UI слой сильно связан с конкретными типами сущностей
- Изменения в сущностях могут требовать изменений в UI
- Сложнее тестировать UI независимо от сущностей

**Решение:**
- Использовать DTO (Data Transfer Objects) для передачи данных
- Или использовать forward declarations где возможно
- Или создать интерфейсы для сущностей

---

## 📊 Оценка соответствия по слоям

### Entities (Сущности) - ✅ 100%
- Только данные и валидация
- Нет зависимостей от других слоев
- Чистая архитектура

### Services (Сервисы) - ✅ 95%
- Хорошее разделение ответственности
- Централизация файловых операций
- Небольшое нарушение: возврат прямых ссылок через EstateAgency

### Core (Ядро) - ⚠️ 70%
- Хорошая координация менеджеров
- Нарушение: возврат прямых ссылок на менеджеры
- Нет фасадных методов для UI

### UI (Интерфейс) - ⚠️ 60%
- Доступ через EstateAgency ✅
- Но: прямые зависимости от менеджеров в MainWindow ❌
- Использование dynamic_cast ❌
- Прямые includes сущностей ❌

---

## 🎯 Приоритеты исправления

### Высокий приоритет:
1. Убрать прямые зависимости от менеджеров в `MainWindow`
2. Добавить фасадные методы в `EstateAgency`

### Средний приоритет:
3. Убрать использование `dynamic_cast` (Visitor Pattern)
4. Унифицировать обработку исключений

### Низкий приоритет:
5. Внедрить DTO для передачи данных
6. Использовать forward declarations где возможно

---

## 💡 Рекомендации

1. **Фасадный паттерн для EstateAgency**
   - Добавить методы типа `addProperty()`, `findProperty()`, `removeProperty()`
   - UI работает только через эти методы
   - Менеджеры остаются скрытыми

2. **Visitor Pattern для типов недвижимости**
   - Создать `PropertyVisitor` интерфейс
   - Каждый тип недвижимости реализует `accept(Visitor&)`
   - UI использует visitor для получения специфичной информации

3. **Общий базовый класс исключений**
   - `ServiceException` как базовый класс
   - Все исключения менеджеров наследуются от него
   - UI ловит только `ServiceException`

4. **DTO для передачи данных**
   - Создать `PropertyDTO`, `ClientDTO` и т.д.
   - UI работает только с DTO
   - Менеджеры преобразуют Entity ↔ DTO

---

## Заключение

Архитектура проекта в целом хорошая, но есть несколько мест, где можно улучшить разделение слоев. Основные проблемы связаны с тем, что UI слой слишком много знает о внутренней структуре других слоев. Исправление этих нарушений сделает код более поддерживаемым и тестируемым.


